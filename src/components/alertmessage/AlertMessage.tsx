import React, { useEffect } from "react";

const AlertMessage = () => {
  //   const dispatch = useDispatch();
  //   const mess = useSelector((state) => state.message);

  //   useEffect(() => {
  //     if (mess.display) {
  //       switch (mess.type) {
  //         case 'success':
  //           toast.success(mess.message);
  //           break;
  //         case 'delete':
  //         case 'error':
  //           toast.error(mess.message);
  //           break;
  //         default:
  //           break;
  //       }

  //       // Réinitialiser l'état du message
  //       dispatch(
  //         message({
  //           display: false,
  //           message: '',
  //           type: '',
  //         })
  //       );
  //     }
  //   }, [mess, dispatch]); // Exécute l'effet uniquement lorsque `mess` change

  return <>dqsd</>;
};

export default AlertMessage;
